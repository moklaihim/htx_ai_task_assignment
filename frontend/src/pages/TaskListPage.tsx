// Full implementation lands in task 4.4 (design §6.2). Stub here only proves
// routing (task 4.1) navigates without a full page reload.
export function TaskListPage() {
  return (
    <main>
      <h1>Task List</h1>
    </main>
  );
}
